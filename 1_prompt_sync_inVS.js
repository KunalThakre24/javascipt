/**
 *todo;
 **  1.  npm init
 **  2.  enter enter
 *?  3.  is this ok ? (yes) enter
 *!  4.  npm install prompt-sync
 *todo:  and in the last, add this =>  const prompt = require("prompt-sync")();
 *?     on top of the file
 ** that's all
 */

//*?  template literal
//  console.log(` djlkfkldjlf ${a-b} djl fdjl`);
