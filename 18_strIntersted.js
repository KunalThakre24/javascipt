let arr = ["bhopal", "patna", "bihar", "states of america"]

function longestName(arr) {
    let ansIdx = 0;
    for (let i = 0; i < arr.length; i++) {
        let currEleIdx = arr[i].length;
        let ansIdx = arr[ansIdx].length;
        if (currEleIdx > ansIdx) {

        }
    }
}